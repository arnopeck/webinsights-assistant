# Legacy pasted content

The original `pasted_content.txt` file contained copied contest/reference material from the old project phase.

It was intentionally removed from the repository root during cleanup. The exact original content remains available in the Git history, but it is no longer part of the active project tree because the current product direction is a self-hosted PHP web application rather than the previous ADK/Python contest-oriented prototype.
